info:
  name: Inscription
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/inscription
  body:
    type: json
    data: |-
      {
        "email": "test@test.com",
        "password": "1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
