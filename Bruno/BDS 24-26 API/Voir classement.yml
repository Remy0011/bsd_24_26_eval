info:
  name: Voir classement
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:8080/sessions/1/classement
  auth:
    type: bearer
    token: |-
      [
        { "questionId": 1, "prix": 75 },
        { "questionId": 2, "prix": 120 }
      {{token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
