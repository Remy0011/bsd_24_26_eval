info:
  name: Lister sessions
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/sessions
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
