info:
  name: Rejoindre session
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/sessions/1/join
  auth:
    type: bearer
    token: |-
      {
        "nom": "Session 1"
      {
        "nom": "Session 1"
      {{token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
